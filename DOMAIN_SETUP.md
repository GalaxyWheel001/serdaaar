# 📋 Инструкция по настройке кастомного домена в Netlify

## ✅ Что уже сделано:

1. ✅ NS записи установлены на ваш домен
2. ✅ Конфигурация клоакинга обновлена для поддержки `aslanerturk.com`

## 🔧 Что нужно сделать в Netlify Dashboard:

### Шаг 1: Добавить кастомный домен

1. Войдите в **Netlify Dashboard** → выберите ваш сайт (`penalibabasi`)
2. Перейдите в **Site settings** → **Domain management**
3. Нажмите **Add custom domain**
4. Введите ваш домен: `aslanerturk.com`
5. Нажмите **Verify**

### Шаг 2: Настроить DNS записи (если нужно)

Если Netlify не автоматически определил DNS записи:

1. В **Domain management** найдите ваш домен `aslanerturk.com`
2. Netlify покажет необходимые DNS записи
3. Убедитесь, что в вашем DNS провайдере установлены:
   - **A записи** (если требуется)
   - **CNAME записи** (если требуется)

### Шаг 3: Настроить SSL сертификат

1. Netlify автоматически выдаст SSL сертификат через Let's Encrypt
2. Это может занять от нескольких минут до 24 часов
3. Проверьте статус в **Domain management** → **HTTPS**

### Шаг 4: Настроить редирект www → без www (опционально)

Если хотите, чтобы `www.aslanerturk.com` редиректил на `aslanerturk.com`:

1. В **Domain management** добавьте `www.aslanerturk.com`
2. Netlify автоматически настроит редирект

Или добавьте в `netlify.toml`:

```toml
[[redirects]]
  from = "https://www.aslanerturk.com/*"
  to = "https://aslanerturk.com/:splat"
  status = 301
  force = true
```

## 🎯 Как это работает:

### Текущая логика клоакинга:

1. **Боты/модераторы** (Facebook, Google и т.д.):
   - Обнаруживаются по User-Agent и IP
   - Редиректятся на: `yalanyokgaming.netlify.app` (безопасная страница)

2. **Реальные пользователи**:
   - На `aslanerturk.com` → видят квиз
   - На `penalibabasi.netlify.app` → видят квиз
   - После завершения квиза → редирект на Telegram

## 📝 Поддерживаемые домены:

- ✅ `aslanerturk.com` (кастомный домен)
- ✅ `www.aslanerturk.com` (с www)
- ✅ `penalibabasi.netlify.app` (Netlify домен)

Все эти домены работают как входные точки и показывают квиз.

## ⚠️ Важные замечания:

1. **Распространение DNS**: После установки NS записей может потребоваться до 48 часов для полного распространения DNS по всему миру

2. **SSL сертификат**: Netlify автоматически выдаст SSL, но это может занять время

3. **Проверка работы**: После настройки проверьте:
   - `https://aslanerturk.com` - должен показывать квиз
   - Боты должны редиректиться на `yalanyokgaming.netlify.app`

4. **Логи**: Проверяйте логи Edge Functions в Netlify Dashboard для отладки

## 🔍 Проверка настройки:

После завершения настройки проверьте:

```bash
# Проверка DNS
nslookup aslanerturk.com

# Проверка SSL
curl -I https://aslanerturk.com

# Проверка редиректа ботов (симуляция Facebook бота)
curl -H "User-Agent: facebookexternalhit/1.1" https://aslanerturk.com
```

## 📞 Если что-то не работает:

1. Проверьте логи Edge Functions в Netlify Dashboard
2. Убедитесь, что DNS записи правильно настроены
3. Подождите до 48 часов для распространения DNS
4. Проверьте, что SSL сертификат выдан

---

**Готово!** После выполнения этих шагов ваш кастомный домен `aslanerturk.com` будет работать с системой клоакинга.



